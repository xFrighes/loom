# Loom Benchmark Dashboard

Run current numbers with:

```bash
bun run --filter @loom-kit/compiler benchmark -- --output docs/benchmark-dashboard.md
```

The dashboard reports compiler speed, generated output size, and rebuild latency for React, Vue, and Svelte against handwritten baseline snippets.
