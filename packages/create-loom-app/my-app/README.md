# my-app

Generated with `create-loom-app` using the React starter.

Vite + React with a typed Loom component entrypoint.

## Development

```bash
bun install
bun run dev
```

Edit `src/App.loom` to start shaping the app. Loom keeps declarations in zones so state, logic, markup, and styles stay easy to scan.
