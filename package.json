{
  "name": "loom",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "verify": "node scripts/verify.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
